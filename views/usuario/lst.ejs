<%- include('../partials/header') %>

<div class="container">
  <h1 class="title">Usuários Cadastrados</h1>

  <form method="get" action="/usuario" class="search-bar">
    <input type="text" name="busca" class="form-control" placeholder="Buscar por nome" value="<%= busca %>">
    <div class="col-md-3">
<button class="btn btn-primary w-100">Filtrar</button>
</div>
  </form>

  <div class="actions">
    <a href="/usuario/add" class="btn btn-add">+ Adicionar Usuário</a>
  </div>

  <table class="table">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Email</th>
      </tr>
    </thead>

    <tbody>
      <% usuarios.forEach(u => { %>
        <tr>
          <td><%= u.nome %></td>
          <td><%= u.email %></td>
          <td>
            <a href="/usuario/edit/<%= u._id %>" class="btn btn-edit">Editar</a>
            <a href="/usuario/delete/<%= u._id %>" class="btn btn-delete" onclick="return confirm('Excluir este usuário?')">Excluir</a>
          </td>
        </tr>
      <% }) %>

      <% if (usuarios.length === 0) { %>
        <tr>
          <td colspan="3" class="empty">Nenhum usuário encontrado.</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

<%- include('../partials/footer') %>
